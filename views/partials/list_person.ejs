<!DOCTYPE html>
<html>

<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <style>
    * {
      box-sizing: border-box
    }

    body {
      font-family: "Lato";
    }

    /* Style the tab */
    .tab {
      float: left;

      background-color: #c0dfef;
      width: 30%;
      height: 100%;
      position: absolute;

    }

    /* Style the buttons inside the tab */
    .tab button {
      display: block;
      background-color: inherit;
      color: black;
      padding: 22px 16px;
      width: 100%;
      border: none;
      outline: none;
      text-align: left;
      cursor: pointer;
      transition: 0.3s;
      font-size: 17px;
    }

    .tab a {
      text-decoration: none;
      color: black;
    }

    /* Change background color of buttons on hover */
    .tab button:hover {
      background-color: #26c4e2;
    }

    /* Create an active/current "tab button" class */
    .tab button.active {
      background-color: #77b5fe;
    }

    /* Style the tab content */
    .tabcontent {
      float: left;
      padding: 0px 12px;
      width: 70%;
      border-left: none;
      height: 0px;
    }
  </style>
</head>

<body>
  <h2></h2>

  <% if (doc) { %>
    <font size="20" color="#77b5fe" face=" Helvetica">Liste des patients</font>
    <div class="tab">
      <center>
        <font color="#77b5fe" size="6">Selectionner le patient</font>
        <a href="/add"><i class="fa fa-plus"></i></a>
      </center>
      <% } else if (admin) { %>
        <font size="20" color="#77b5fe" face=" Helvetica">Désigner Docteur</font>
        <div class="tab">
          <center>
            <a href="/add"><i class="fa fa-plus"></i></a>
          </center>
          <% } else { %>
            <font size="20" color="#77b5fe" face=" Helvetica">Liste des docteurs</font>
            <div class="tab">
              <center>
                <font color="#77b5fe" size="6">Selectionner le docteur</font>
              </center>
              <% } %>

                <% if (list) { %>
                  <% for (person of list) { %>
                    <% if (doc) { %>
                      <a href="/portal?numsecu=<%= person.numsecu %>">
                        <button class="tablinks">
                          <%= person.nom %>
                            <%= person.prenom %>
                        </button>
                      </a><br />
                      <% } else { %>
                        <a href="/portal?numsecu=<%= person.numsecu %>">
                          <button class="tablinks">
                            <%= person.nom %>
                              <%= person.prenom %>
                          </button>
                        </a><br />
                        <% }; %>
                          <% }; %>
                            <% }; %>
            </div>


</body>

</html>